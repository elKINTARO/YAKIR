/// <reference types="nativewind/types" />

// The Tailwind entry file is consumed by the Metro transformer, not by
// TypeScript, so it only needs a side-effect module declaration.
declare module '*.css';
