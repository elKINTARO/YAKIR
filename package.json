{
  "name": "yakir",
  "version": "0.0.0",
  "private": true,
  "description": "Anxiety self-observation app built around prediction testing",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "typescript": "~6.0.3"
  }
}
